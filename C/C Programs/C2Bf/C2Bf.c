#include<stdio.h>
int main(int argc, char**argv)
{
	int x = 30, y = 40;
	if (x == y)
		printf("x is equal to y");
	else if (x > y)
		printf("x is greater than Y");
	else if (x < y)
	{
		printf("x is less then y");
	}

	return 0;
}