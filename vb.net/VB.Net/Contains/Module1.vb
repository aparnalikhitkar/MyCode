Module Containss

    Sub Main()
        Dim a As String = "this is a test "
        If (a.Contains("is")) Then
            Console.WriteLine("The sequence 'is' was found." + a)
        End If
        Console.ReadLine()
    End Sub

End Module
